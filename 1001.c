#include <stdio.h>
 
int main() 
{
 
    int A, B, X;
    
    printf("Write the number for A: ");
    scanf("%d", &A);
    
    printf("Write the number for B: ");
    scanf("%d", &B);
    
    X = A + B;
    
    printf("The result of sum is: %d\n", X);
 
    return 0;
}